# AgentBot — План задач (TODO)

**Дата создания:** 1 марта 2026 г.  
**Статус проекта:** Частично реализован (требуется завершение ключевых компонентов)

---

## 🔴 Критические задачи (блокируют работу)

- [x] **Реализовать `MessageProcessor.cs`**  
  **Статус:** Выполнено  
  **Описание:** Класс для маршрутизации сообщений между CommandHandler и IAiAgent. Требуется:
  - Добавить конструктор с инъекцией зависимостей (CommandHandler, IAiAgent, IBotProvider, ILogger, IEnumerable\<IToolFunction\>)
  - Реализовать метод `ProcessAsync(Message message)`:
    - Если сообщение начинается с "/" → вызвать `CommandHandler.HandleCommandAsync()`
    - Иначе → вызвать `IAiAgent.ProcessMessageAsync()` и отправить ответ через `IBotProvider.SendMessageAsync()`
  - Добавить логирование действий
  **Файл:** `Handlers/MessageProcessor.cs`

- [x] **Исправить `TelegramBotProvider.cs`**  
  **Статус:** Выполнено  
  **Описание:** В классе есть дублирующие методы `StartPollingAsync()` и `SendMessageAsync()` без параметров, которые выбрасывают `NotImplementedException`. Требуется:
  - Удалить дублирующие методы (без CancellationToken)
  - Исправить свойство `OnMessageReceived` (сейчас выбрасывает NotImplementedException)
  - Убедиться, что интерфейс IBotProvider соответствует реализации
  **Файл:** `Bots/TelegramBotProvider.cs`

- [x] **Настроить `appsettings.json`**  
  **Статус:** Выполнено  
  **Описание:** Добавить необходимые секции:
  - `Bots:Telegram:ApiToken` — токен Telegram-бота
  - `AiAgent:ApiKey` — API-ключ Google Gemini
  - `AiAgent:Model` — модель (gemini-1.5-pro)
  - `WeatherApiKey` — ключ OpenWeatherMap (для WeatherTool)
  - `AppBaseDir` — базовая директория (для LinuxCMDTool)
  - Секция Serilog для логирования в файл
  **Файл:** `appsettings.json`

- [x] **Обновить `Program.cs`**  
  **Статус:** Выполнено  
  **Описание:** Требуется:
  - Зарегистрировать `MessageProcessor` в DI
  - Зарегистрировать все инструменты (IToolFunction) через `IEnumerable<IToolFunction>`
  - Добавить регистрацию `IHttpClientFactory` для WeatherTool
  - Настроить Serilog с ротацией файлов
  **Файл:** `Program.cs`

---

## 🟡 Важные задачи (необходимы для функциональности)

- [x] **Завершить `DatabaseTool.cs`**  
  **Статус:** Выполнено  
  **Описание:** Реализовать инструмент для работы с SQLite:
  - Методы: `save_note`, `get_note`, `list_notes`, `delete_note`
  - Использование Microsoft.Data.Sqlite
  - Хранение заметок пользователей в локальной БД
  **Файл:** `Tools/DatabaseTool.cs`

- [x] **Реализовать `GrokAgent.cs`**  
  **Статус:** Выполнено  
  **Описание:** Реализация IAiAgent для Grok (xAI API):
  - Интеграция с xAI API (аналогично OpenAI)
  - Поддержка function calling
  - Конфигурация через appsettings.json (ApiUrl, ApiKey, Model)
  **Файл:** `AiAgents/GrokAgent.cs`

- [x] **Реализовать `OpenAiAgent.cs`**  
  **Статус:** Выполнено  
  **Описание:** Реализация IAiAgent для OpenAI:
  - Использование OpenAI SDK или HTTP API
  - Поддержка function calling
  - Конфигурация через appsettings.json (ApiKey, Model)
  **Файл:** `AiAgents/OpenAiAgent.cs`

- [x] **Добавить выбор провайдера ИИ в `Program.cs`**  
  **Статус:** Выполнено  
  **Описание:** Реализовать фабрику или switch для выбора ИИ-агента на основе конфигурации:
  ```json
  "AiAgent": {
    "Provider": "Gemini", // или "OpenAI", "Grok"
    "ApiKey": "...",
    "Model": "..."
  }
  ```
  **Файл:** `Program.cs`

- [x] **Добавить разделение истории чатов по `chatId`**  
  **Статус:** Выполнено  
  **Описание:** В `GeminiAiAgent` (и других агентах) использовать `ConcurrentDictionary<long, List<Content>>` для хранения истории отдельно для каждого чата. Добавить очистку старых сообщений для предотвращения утечек памяти.
  **Файл:** `AiAgents/GeminiAiAgent.cs`

---

## 🟢 Задачи по улучшению (опционально)

- [x] **Добавить команды в `CommandHandler.cs`**  
  **Статус:** Выполнено  
  **Описание:** Расширить список команд:
  - `/weather <город>` — получить погоду (вызов WeatherTool)
  - `/note <текст>` — сохранить заметку (вызов DatabaseTool)
  - `/cmd <команда>` — выполнить Linux-команду (вызов LinuxCMDTool)
  - `/stats` — статистика использования бота
  **Файл:** `Handlers/CommandHandler.cs`

- [x] **Реализовать `OnMessageReceived` в `IBotProvider`**  
  **Статус:** Выполнено  
  **Описание:** Добавить поддержку колбека для альтернативного способа обработки сообщений.
  **Файл:** `Interfaces/IBotProvider.cs`, `Bots/TelegramBotProvider.cs`

- [x] **Добавить Polly для ретраев**  
  **Статус:** Выполнено  
  **Описание:** Настроить политики повторных попыток для:
  - Запросов к Telegram API
  - Запросов к Google Gemini API
  - Запросов к внешним API (WeatherTool)
  **Файл:** `Bots/TelegramBotProvider.cs`, `AiAgents/GeminiAiAgent.cs`, `Tools/WeatherTool.cs`

- [x] **Добавить поддержку других типов сообщений**  
  **Статус:** Реализовано базовое распознавание  
  **Описание:** Расширить `TelegramBotProvider.HandleUpdateAsync` для обработки:
  - Фотографий (логирование и ответ)
  - Голосовых сообщений (логирование и ответ)
  - Документов (логирование и ответ)
  **Файл:** `Bots/TelegramBotProvider.cs`

- [ ] **Добавить Unit-тесты**  
  **Статус:** Тестов нет  
  **Описание:** Создать проект тестов (xUnit/NUnit) с моками для:
  - CommandHandler (тестирование команд)
  - Tools (тестирование ExecuteAsync)
  - MessageProcessor (тестирование маршрутизации)
  **Файл:** `AgentBot.Tests/` (новый проект)

- [ ] **Добавить интеграционные тесты**  
  **Статус:** Тестов нет  
  **Описание:** Тесты с реальным ботом (test bot token) и моком ИИ-API.
  **Файл:** `AgentBot.Tests/Integration/`

- [ ] **Создать systemd unit-файл**  
  **Статус:** Не создан  
  **Описание:** Подготовить шаблон `/etc/systemd/system/agentbot.service` для развёртывания на Linux.
  **Файл:** `agentbot.service` (корень проекта)

- [ ] **Добавить Docker Compose**  
  **Статус:** Только Dockerfile  
  **Описание:** Создать `docker-compose.yml` для удобного развёртывания с переменными окружения.
  **Файл:** `docker-compose.yml`

- [ ] **Добавить README.md**  
  **Статус:** Нет  
  **Описание:** Создать документацию для пользователей:
  - Описание проекта
  - Быстрый старт
  - Конфигурация
  - Примеры использования
  **Файл:** `README.md`

---

## 🔵 Задачи по безопасности

- [x] **Добавить проверку прав доступа к командам**  
  **Статус:** Выполнено  
  **Описание:** RBAC через `AccessControlService`. Админы регистрируются по паролю (/register). Список сохраняется в admins.json. Команды /cmd и /stats только для админов.
  **Файл:** `Security/AccessControlService.cs`, `Handlers/CommandHandler.cs`

- [ ] **Добавить rate limiting**  
  **Статус:** Нет ограничений  
  **Описание:** Ограничить количество запросов от одного пользователя в минуту (защита от злоупотреблений).
  **Файл:** `Handlers/MessageProcessor.cs`

- [ ] **Добавить логирование подозрительных действий**  
  **Статус:** Базовое логирование  
  **Описание:** Логировать попытки выполнения запрещённых команд, превышение лимитов, ошибки аутентификации.
  **Файл:** По всему проекту

---

## 📊 Сводка по статусу

| Категория | Задач | Выполнено | В процессе | Осталось |
|-----------|-------|-----------|------------|----------|
| 🔴 Критические | 4 | 4 | 0 | 0 |
| 🟡 Важные | 6 | 5 | 0 | 1 |
| 🟢 Улучшение | 10 | 4 | 0 | 6 |
| 🔵 Безопасность | 3 | 1 | 0 | 2 |
| **Итого** | **23** | **14** | **0** | **9** |

---

## Приоритетный порядок выполнения

1. **MessageProcessor.cs** — без этого бот не работает
2. **TelegramBotProvider.cs** — исправление дублирования методов
3. **appsettings.json** — конфигурация для запуска
4. **Program.cs** — полная регистрация DI
5. **DatabaseTool.cs** — базовая функциональность tools
6. **Остальные задачи** — по приоритету

---

## Заметки

- Все задачи протестировать после выполнения
- После завершения критических задач — проверить сборку: `dotnet build`
- После завершения важных задач — протестировать локально
- Перед развёртыванием — выполнить задачи по безопасности
